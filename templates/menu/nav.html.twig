<div class="flex_between flex_div">
    <h1><a href="{{ path('main_home') }}"> <img src="{{ asset('img/logo.svg') }}" alt="Series !"></a></h1>
    <nav>
        <ul>
            <li><a href="{{ path('main_home') }}" title="Home">Home</a></li>
            <li><a href="{{ path('serie_list', {'page' : 1}) }}" title="All series">All series</a></li>
            {% if app.user %}
                <li><a href="{{ path('serie_create') }}" title="Add a new serie">Create serie</a></li>
                <li><a href="{{ path('season_create') }}" title="Add a new season">Create season</a></li>
                {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('admin_main') }}" title="For admin only">Administrate</a></li>

            {% endif %}
                <li><a href="{{ path('app_logout') }}" title="Logout">Logout ({{ app.user.firstname }})</a></li>
            {% else %}
                <li><a href="{{ path('app_login') }}" title="Login">Login</a></li>
                <li><a href="{{ path('app_register') }}" title="Register">Register</a></li>
            {% endif %}
        </ul>
    </nav>
</div>
